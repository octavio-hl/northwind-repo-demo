version: 2

models:
  - name: dim_funcionarios
    description: Dimensão com dados de funcionários 
    columns:
      - name: sk_funcionario
        description: Essa é a chave surrogate key da tabela
        tests:
          - unique
          - not_null
              
       